"""
api

FastAPI routes only, per the developer handbook.

This package must never contain business logic, scoring logic, or
direct AI/LLM calls. Routes here should only: validate requests,
call into services/core, and return responses.
"""
