const steps=["Discover","Evaluate","Improve"]; export function WorkflowSteps({current}:{current:1|2|3}){return <ol className="grid grid-cols-3 gap-2">{steps.map((s,i)=>{const n=i+1;return <li key={s} className={`rounded-lg border px-3 py-2 text-center text-xs font-medium ${n===current?"border-signal-600 bg-signal-100 text-signal-600":"border-surface-2 bg-surface-1 text-ink-400"}`}><span className="mr-1 font-mono">{n<current?"✓":`0${n}`}</span>{s}</li>})}</ol>}